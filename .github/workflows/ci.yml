name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest flake8 pytorch-msssim lpips
          echo "Installed dependencies: $(pip list | grep -E 'flake8|pytest|pytorch-msssim|lpips')"
      - name: Check memory
        run: free -h
      - name: Run linter
        run: |
          echo "Running flake8 on src/ and tests/"
          flake8 src/ tests/ || { echo "Flake8 failed"; exit 1; }
      - name: Run tests
        run: |
          cd $GITHUB_WORKSPACE
          export PYTHONPATH=$PYTHONPATH:$GITHUB_WORKSPACE/src
          pytest tests/ --rootdir=$GITHUB_WORKSPACE -s  # -s для вывода print
        timeout-minutes: 10  # Увеличим лимит до 10 минут
